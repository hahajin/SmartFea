services:
  - type: web
    name: truss-backend
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: truss-db
          property: connectionString
      - key: HF_API_KEY
        sync: false